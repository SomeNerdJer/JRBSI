<Window x:Class="JRBSI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:JRBSI.Models"
        Title="Jeremy's Robotics Bulk Software Installer v1.5.1"
        Height="560"
        Width="720"
        MinHeight="480"
        MinWidth="640"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <Style x:Key="PackageCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{DynamicResource ProgressTrackBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ProgressFillBrush}" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource HeaderBackgroundBrush}"
                CornerRadius="10"
                Padding="20"
                Margin="0,0,0,20">
            <Grid>
                <Grid Margin="0,0,40,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0"
                           Source="pack://application:,,,/Assets/app.ico"
                           Width="36"
                           Height="36"
                           Margin="0,2,12,0"
                           VerticalAlignment="Top"
                           RenderOptions.BitmapScalingMode="HighQuality" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Jeremy's Robotics Bulk Software Installer v1.5.1"
                                   FontSize="22"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource HeaderTitleBrush}"
                                   TextWrapping="Wrap" />
                        <TextBlock Text="Property of Jeremedia LLC  •  Jeremedia.net"
                                   FontSize="13"
                                   Foreground="{DynamicResource HeaderSubtitleBrush}"
                                   Margin="0,6,0,0" />
                        <TextBlock Text="Running as Administrator — installers will not prompt for UAC individually."
                                   FontSize="12"
                                   Foreground="{DynamicResource HeaderNoteBrush}"
                                   Margin="0,10,0,0"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

                <Button x:Name="ThemeToggleButton"
                        Width="32"
                        Height="32"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Cursor="Hand"
                        Click="ThemeToggle_Click"
                        ToolTip="Switch to light mode"
                        Background="Transparent"
                        BorderThickness="0"
                        Padding="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="ThemeToggleChrome"
                                    Width="32"
                                    Height="32"
                                    CornerRadius="16"
                                    Background="#22FFFFFF">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ThemeToggleChrome" Property="Background" Value="#44FFFFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Grid Width="18" Height="18">
                        <Path x:Name="SunIcon"
                              Fill="#FFD54F"
                              Stretch="Uniform"
                              Data="M12,7A5,5 0 0,1 17,12A5,5 0 0,1 12,17A5,5 0 0,1 7,12A5,5 0 0,1 12,7M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,2L14.39,5.42C13.65,5.15 12.84,5 12,5C11.16,5 10.35,5.15 9.61,5.42L12,2M3.34,7L7.5,6.65C6.9,7.16 6.36,7.78 5.94,8.5C5.5,9.24 5.25,10 5.11,10.79L3.34,7M3.36,17L5.12,13.23C5.26,14 5.53,14.78 5.95,15.5C6.37,16.24 6.91,16.86 7.5,17.37L3.36,17M20.65,7L18.88,10.79C18.74,10 18.47,9.23 18.05,8.5C17.63,7.78 17.1,7.15 16.5,6.64L20.65,7M20.64,17L16.5,17.36C17.09,16.85 17.62,16.22 18.04,15.5C18.46,14.77 18.73,14 18.87,13.21L20.64,17M12,22L9.59,18.56C10.33,18.83 11.14,19 12,19C12.82,19 13.63,18.83 14.37,18.56L12,22Z" />
                        <Path x:Name="MoonIcon"
                              Fill="#E8EEF7"
                              Stretch="Uniform"
                              Visibility="Collapsed"
                              Data="M21,12.79A9,9 0 1,1 11.21,3 7,7 0 0,0 21,12.79Z" />
                    </Grid>
                </Button>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1"
                   Text="The following software will be installed silently. Items already on this PC are marked automatically."
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource BodyTextBrush}"
                   Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PackagesItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:InstallItem}">
                        <Border Style="{StaticResource PackageCardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0"
                                           Grid.Column="0"
                                           Text="{Binding Name}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource CardTitleBrush}" />

                                <TextBlock Grid.Row="0"
                                           Grid.Column="1"
                                           Text="{Binding StatusText}"
                                           FontWeight="Bold"
                                           Foreground="{Binding StatusBrush}"
                                           VerticalAlignment="Center" />

                                <ProgressBar Grid.Row="1"
                                             Grid.ColumnSpan="2"
                                             Height="18"
                                             Margin="0,12,0,0"
                                             Minimum="0"
                                             Maximum="100"
                                             Value="{Binding Progress}"
                                             IsIndeterminate="{Binding IsIndeterminate}" />

                                <TextBlock Grid.Row="2"
                                           Grid.ColumnSpan="2"
                                           Text="{Binding DetailMessage}"
                                           FontSize="12"
                                           Foreground="{DynamicResource CardDetailBrush}"
                                           Margin="0,8,0,0"
                                           TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock x:Name="SummaryTextBlock"
                   Grid.Row="3"
                   Text="Ready"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource SummaryTextBrush}"
                   Margin="0,8,0,16" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="InstallButton"
                    Content="Install"
                    Width="140"
                    Height="40"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Background="{DynamicResource ButtonBackgroundBrush}"
                    Foreground="{DynamicResource ButtonForegroundBrush}"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="InstallButton_Click" />
        </StackPanel>
    </Grid>
</Window>
